# Project Rules
- 코드 수정 전에 항상 최신 상태인지 확인해줘, 최신이 아니라면 pull로 최신 코드로 sync-up 한 뒤 이어서 작업해줘. 
- 코드 변경 시 DB/스키마 마이그레이션 파일은 만들지 않는다.
- 마이그레이션은 사용자가 명시적으로 요청한 경우에만 작성하거나 제안한다.
- if / while / for 등 제어문의 본문은 한 줄이어도 항상 중괄호({})로 감싼다 (값을 반환하는 if/when 표현식은 제외).